import { Routes } from '@angular/router';
import { TodoBoardComponent } from './components/todo-board/todo-board.component';

export const routes: Routes = [{ path: '', component: TodoBoardComponent }];
